What is the difference between a merge and a rebase?
A merge creates a commit and also keeps both the branches' commit history. On the other hand, rebase will rewrite the commit history and does not give a merge commit.

